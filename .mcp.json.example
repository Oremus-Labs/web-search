{
  "note": "This repo no longer ships an MCP server. Use the web-search CLI instead.",
  "example": {
    "command": "npx",
    "args": ["-y", "@oremus-labs/web-search", "search", "example domain"],
    "env": {
      "WEB_SEARCH_API_URL": "https://web-search.oremuslabs.app"
    }
  }
}
