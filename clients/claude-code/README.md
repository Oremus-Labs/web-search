# Claude Code setup

1. Install Node.js 20+
2. Install or run `web-search`
3. Set `WEB_SEARCH_API_URL` if you are not using the default public API

Example:

```bash
web-search search "example domain"
```
